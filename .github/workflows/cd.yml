name: CD

on:
  push:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'docs/**'
      - '.vscode/**'
      - '.editorconfig'

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: false

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '10'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  ci-check:
    name: CI Checks
    uses: ./.github/workflows/ci.yml

  docker:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: ci-check
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      image-digest: ${{ steps.build-push.outputs.digest }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}},enable=${{ startsWith(github.ref, 'refs/tags/v') }}

      - name: Build and push Docker image
        id: build-push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NODE_VERSION=${{ env.NODE_VERSION }}

  # deploy-staging:
  #   name: Deploy to Staging
  #   runs-on: ubuntu-latest
  #   needs: docker
  #   environment:
  #     name: staging
  #     url: https://staging.your-domain.com
  #   steps:
  #     - name: Deploy
  #       run: |
  #         echo "Deploy image ${{ needs.docker.outputs.image-tag }} to staging"
  #         # Add your deployment commands here, for example:
  #         # - SSH deploy: ssh user@server "docker pull $IMAGE && docker-compose up -d"
  #         # - Railway: railway up
  #         # - Render: curl deploy hook
  #         # - AWS ECS: aws ecs update-service ...
  #         # - Kubernetes: kubectl set image ...

  # deploy-production:
  #   name: Deploy to Production
  #   runs-on: ubuntu-latest
  #   needs: [docker, deploy-staging]
  #   environment:
  #     name: production
  #     url: https://your-domain.com
  #   steps:
  #     - name: Deploy
  #       run: |
  #         echo "Deploy image ${{ needs.docker.outputs.image-tag }} to production"
